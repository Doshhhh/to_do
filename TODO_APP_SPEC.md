# Техническое задание: Todo List App

## Обзор проекта

Веб-приложение для управления задачами (Todo List) с категориями и подкатегориями, адаптивным интерфейсом для десктопа и мобильных устройств, авторизацией через Google, светлой и тёмной темами. Стек: Next.js + Tailwind CSS + Framer Motion + Supabase.

---

## 1. Стек технологий

| Слой | Технология |
|------|-----------|
| Фреймворк | Next.js 14+ (App Router) |
| Язык | TypeScript |
| Стили | Tailwind CSS |
| Анимации | Framer Motion |
| UI-компоненты | shadcn/ui (опционально, для консистентности) |
| Drag & Drop | @dnd-kit/core + @dnd-kit/sortable |
| БД + Auth + API | Supabase (PostgreSQL, Auth, Row Level Security) |
| OAuth-провайдер | Google (через Supabase Auth) |
| Деплой фронта | Vercel |
| Иконки | Lucide React |

---

## 2. Цветовая палитра (стиль Claude)

### Светлая тема

| Назначение | Цвет | HEX |
|-----------|------|-----|
| Фон основной | Тёплый бежевый | `#FAF9F6` |
| Фон сайдбара | Чуть темнее бежевого | `#F0EDE8` |
| Фон карточки задачи | Белый | `#FFFFFF` |
| Текст основной | Тёмно-коричневый | `#2D2A26` |
| Текст вторичный | Серо-коричневый | `#8C8579` |
| Акцент (primary) | Терракотовый | `#D97706` |
| Акцент hover | Тёмный терракот | `#B45F04` |
| Успех (выполнено) | Приглушённый зелёный | `#6B8F71` |
| Разделители | Светло-бежевый | `#E5E0D8` |

### Тёмная тема

| Назначение | Цвет | HEX |
|-----------|------|-----|
| Фон основной | Тёмно-коричневый | `#1A1816` |
| Фон сайдбара | Чуть светлее | `#242220` |
| Фон карточки задачи | Тёмно-серый | `#2D2B28` |
| Текст основной | Светло-бежевый | `#E8E4DE` |
| Текст вторичный | Приглушённый бежевый | `#9C958B` |
| Акцент (primary) | Тёплый оранжевый | `#E8943A` |
| Акцент hover | Светлый оранжевый | `#F0A85C` |
| Успех (выполнено) | Приглушённый зелёный | `#7FA886` |
| Разделители | Тёмный сепаратор | `#3A3735` |

### Цвета категорий

Все цвета приглушённые, вписываются в тёплую гамму:

| Категория | Цвет (Light) | Цвет (Dark) |
|-----------|-------------|-------------|
| Работа | `#D97706` (терракот) | `#E8943A` |
| Личное | `#8B6F5C` (тёплый коричневый) | `#A68B78` |
| Покупки | `#B07D4B` (карамельный) | `#C9945E` |
| Здоровье | `#6B8F71` (приглушённый зелёный) | `#7FA886` |
| Учёба | `#7C6E8A` (приглушённый фиолетовый) | `#9688A4` |

---

## 3. Структура категорий и подкатегорий

```
📋 Работа (#D97706)
  ├── 💻 Программирование
  ├── 🎬 YouTube
  └── 📌 Другое

📚 Учёба (#7C6E8A)
  ├── 💻 Программирование
  ├── 🇫🇷 Французский
  └── 🇬🇧 Английский

👤 Личное (#8B6F5C)

🛒 Покупки (#B07D4B)

❤️ Здоровье (#6B8F71)
```

Иконки — из Lucide React, стилизованные, минималистичные. Не эмодзи, а SVG-иконки:
- Работа → `Briefcase`
- Учёба → `GraduationCap`
- Личное → `User`
- Покупки → `ShoppingCart`
- Здоровье → `Heart`
- Программирование → `Code`
- YouTube → `Youtube`
- Другое → `MoreHorizontal`
- Французский → `Languages` (или флаг)
- Английский → `Languages` (или флаг)

---

## 4. Макет интерфейса

### Десктоп (≥ 768px)

```
┌──────────────────────────────────────────────────┐
│  Logo    [theme toggle]   [avatar / logout]      │
├─────────┬────────────────────────────────────────┤
│         │  📋 Работа > Программирование          │
│ Sidebar │                                        │
│         │  ┌──────────────────────────────────┐  │
│ [Все]   │  │ ☐ Задача 1          🔴 Высокий  │  │
│         │  │   Дедлайн: 15 мар               │  │
│ Работа  │  └──────────────────────────────────┘  │
│  └ Прог │  ┌──────────────────────────────────┐  │
│  └ YT   │  │ ☐ Задача 2          🟡 Средний  │  │
│  └ Друг │  │   Дедлайн: 20 мар               │  │
│         │  └──────────────────────────────────┘  │
│ Учёба   │                                        │
│  └ Прог │  ── Выполненные ──────────────────── │  │
│  └ FR   │  ┌──────────────────────────────────┐  │
│  └ EN   │  │ ☑ Задача 3          ✅           │  │
│         │  └──────────────────────────────────┘  │
│ Личное  │                                        │
│ Покупки │  [+ Добавить задачу]                   │
│ Здоровье│                                        │
└─────────┴────────────────────────────────────────┘
```

### Мобильный (< 768px)

- Сайдбар скрыт, открывается по нажатию бургер-иконки (☰) в хедере
- Сайдбар выезжает слева поверх контента с затемнением фона (overlay)
- Анимация: slide-in слева (Framer Motion)
- Закрытие: по нажатию на overlay, кнопку X, или свайпу влево

---

## 5. Функционал

### 5.1. Авторизация
- Вход только через Google OAuth (Supabase Auth)
- При первом входе создаётся аккаунт автоматически
- Страница логина: минималистичная, кнопка "Войти через Google"
- После входа — редирект на главную
- Аватарка пользователя из Google-аккаунта в хедере
- Кнопка выхода

### 5.2. Задачи (CRUD)
- **Создание**: кнопка "+ Добавить задачу" внизу списка. Открывает инлайн-форму или модальное окно:
  - Название задачи (обязательно)
  - Описание (опционально, textarea)
  - Категория (обязательно, выпадающий список)
  - Подкатегория (если есть у категории)
  - Приоритет: Высокий / Средний / Низкий (цветные точки: красный, жёлтый, зелёный-серый)
  - Дедлайн (date picker)
- **Редактирование**: клик на задачу раскрывает её для редактирования inline, или открывает модальное окно
- **Удаление**: иконка удаления (без корзины — удаление необратимо, но с confirm-диалогом)
- **Завершение**: чекбокс. При завершении — задача переезжает в секцию "Выполненные" с анимацией
- **Drag & Drop**: перетаскивание задач для ручной сортировки (@dnd-kit)

### 5.3. Отображение задач
- По умолчанию: активные задачи сверху, выполненные — внизу в сворачиваемой секции
- Фильтр по категории: нажатие на категорию в сайдбаре показывает только её задачи
- Фильтр по подкатегории: нажатие на подкатегорию — ещё более узкая фильтрация
- Пункт "Все задачи" в сайдбаре — показывает всё
- Сортировка: по дедлайну, по приоритету, по дате создания (переключатель сортировки)
- Просроченные задачи: подсвечиваются (красноватая обводка или фон)

### 5.4. Сайдбар
- Фиксированный слева (десктоп), бургер-меню (мобайл)
- Категории с иконками и цветными индикаторами
- Подкатегории раскрываются при клике на категорию (анимация раскрытия)
- Счётчик активных задач рядом с каждой категорией/подкатегорией (badge)
- Переключатель темы (иконка солнце/луна)

### 5.5. Темы
- Светлая и тёмная тема
- Переключение: кнопка в хедере или сайдбаре
- Сохранение выбора в localStorage
- По умолчанию: определять по системным настройкам (prefers-color-scheme)
- Плавный transition при переключении

---

## 6. Анимации (Framer Motion)

| Элемент | Анимация |
|---------|----------|
| Карточка задачи — появление | fadeIn + slideUp (из opacity: 0, y: 20) |
| Карточка задачи — удаление | fadeOut + slideLeft + height collapse |
| Карточка задачи — завершение | чекбокс — scale bounce, затем карточка slide down к "Выполненным" |
| Drag & Drop | плавное перемещение с тенью при захвате |
| Сайдбар (мобайл) | slideIn слева (x: -100% → 0) + overlay fadeIn |
| Подкатегории | expand/collapse с height: auto анимацией |
| Модальное окно | fadeIn + scale (0.95 → 1) |
| Переключение темы | плавный transition на всех цветах (CSS transition 200ms) |
| Hover на карточке | subtle scale(1.01) + легкая тень |
| Пустое состояние | bounce анимация иконки |

---

## 7. База данных (Supabase PostgreSQL)

### Таблицы

```sql
-- Профили пользователей (создаётся автоматически при OAuth)
CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  display_name TEXT,
  avatar_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Категории (предзаполнены, но пользователь может добавить свои в будущем)
CREATE TABLE categories (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  name TEXT NOT NULL,
  icon TEXT NOT NULL,          -- название иконки из Lucide (e.g. "Briefcase")
  color_light TEXT NOT NULL,   -- HEX цвет для светлой темы
  color_dark TEXT NOT NULL,    -- HEX цвет для тёмной темы
  sort_order INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Подкатегории
CREATE TABLE subcategories (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  category_id UUID REFERENCES categories(id) ON DELETE CASCADE NOT NULL,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  name TEXT NOT NULL,
  icon TEXT NOT NULL,
  sort_order INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Задачи
CREATE TABLE todos (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  category_id UUID REFERENCES categories(id) ON DELETE SET NULL,
  subcategory_id UUID REFERENCES subcategories(id) ON DELETE SET NULL,
  title TEXT NOT NULL,
  description TEXT,
  priority TEXT CHECK (priority IN ('high', 'medium', 'low')) DEFAULT 'medium',
  is_completed BOOLEAN DEFAULT FALSE,
  completed_at TIMESTAMPTZ,
  deadline DATE,
  sort_order INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Row Level Security (RLS)

```sql
-- Включить RLS на всех таблицах
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE categories ENABLE ROW LEVEL SECURITY;
ALTER TABLE subcategories ENABLE ROW LEVEL SECURITY;
ALTER TABLE todos ENABLE ROW LEVEL SECURITY;

-- Пользователь видит и редактирует только свои данные
CREATE POLICY "Users can manage own profiles"
  ON profiles FOR ALL
  USING (auth.uid() = id);

CREATE POLICY "Users can manage own categories"
  ON categories FOR ALL
  USING (auth.uid() = user_id);

CREATE POLICY "Users can manage own subcategories"
  ON subcategories FOR ALL
  USING (auth.uid() = user_id);

CREATE POLICY "Users can manage own todos"
  ON todos FOR ALL
  USING (auth.uid() = user_id);
```

### Начальные данные (seed)

При первом входе пользователя — создать дефолтные категории и подкатегории через Supabase function или на клиенте:

```
Работа (Briefcase, #D97706/#E8943A)
  → Программирование (Code)
  → YouTube (Youtube)
  → Другое (MoreHorizontal)
Учёба (GraduationCap, #7C6E8A/#9688A4)
  → Программирование (Code)
  → Французский (Languages)
  → Английский (Languages)
Личное (User, #8B6F5C/#A68B78)
Покупки (ShoppingCart, #B07D4B/#C9945E)
Здоровье (Heart, #6B8F71/#7FA886)
```

---

## 8. Структура проекта

```
src/
├── app/
│   ├── layout.tsx              # Root layout, providers (theme, Supabase)
│   ├── page.tsx                # Redirect: auth → dashboard, no auth → login
│   ├── login/
│   │   └── page.tsx            # Login page
│   ├── dashboard/
│   │   └── page.tsx            # Main todo dashboard
│   └── auth/
│       └── callback/
│           └── route.ts        # OAuth callback handler
├── components/
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   └── MobileSidebar.tsx
│   ├── todos/
│   │   ├── TodoList.tsx
│   │   ├── TodoItem.tsx
│   │   ├── TodoForm.tsx        # Create/edit form
│   │   ├── TodoSortControls.tsx
│   │   └── CompletedSection.tsx
│   ├── categories/
│   │   ├── CategoryItem.tsx
│   │   └── SubcategoryItem.tsx
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Modal.tsx
│   │   ├── ConfirmDialog.tsx
│   │   ├── DatePicker.tsx
│   │   ├── PriorityBadge.tsx
│   │   └── ThemeToggle.tsx
│   └── providers/
│       ├── ThemeProvider.tsx
│       └── SupabaseProvider.tsx
├── lib/
│   ├── supabase/
│   │   ├── client.ts           # Browser client
│   │   ├── server.ts           # Server client
│   │   └── middleware.ts       # Auth middleware
│   ├── types.ts                # TypeScript types
│   └── utils.ts                # Helpers
├── hooks/
│   ├── useTodos.ts
│   ├── useCategories.ts
│   └── useTheme.ts
└── middleware.ts                # Next.js middleware for auth protection
```

---

## 9. Безопасность

- **Supabase RLS**: каждый пользователь видит только свои данные на уровне базы данных
- **OAuth**: нет хранения паролей, аутентификация через Google
- **HTTPS**: обеспечивается Vercel автоматически
- **Middleware**: Next.js middleware проверяет аутентификацию перед доступом к dashboard
- **Input validation**: валидация данных на клиенте и через Supabase constraints
- **CSRF**: обеспечивается Supabase Auth SDK
- **Environment variables**: ключи Supabase хранятся в env, anon key — публичный (безопасно с RLS)

---

## 10. Адаптивность

| Breakpoint | Поведение |
|-----------|-----------|
| ≥ 1024px | Полный сайдбар (250px), карточки задач в основной области |
| 768–1023px | Сайдбар уже (200px), компактный вид |
| < 768px | Сайдбар скрыт, бургер-меню, карточки на всю ширину |

---

## 11. Деплой

1. **Supabase**: создать проект на supabase.com, настроить Google OAuth в Authentication → Providers
2. **Vercel**: подключить GitHub-репозиторий, добавить env переменные:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. Настроить домен (опционально)

---

## 12. Будущие улучшения (не реализовывать сейчас)

- Пользовательские категории (добавление/удаление/редактирование)
- Совместные списки (share с другими пользователями)
- Уведомления / напоминания
- PWA (Progressive Web App) для установки на телефон
- Подзадачи
- Повторяющиеся задачи
- Поиск по задачам
- Экспорт данных
